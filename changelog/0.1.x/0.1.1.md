---
summary: "Packaging and documentation pass — LICENSE, bunfig.toml, reverse-domain server name, Docker OCI source label, and a rewritten README."
breaking: false
security: false
---

# 0.1.1 — 2026-03-23

## Added

- **LICENSE** (Apache-2.0), **`bunfig.toml`**, and a **`lint:mcp`** script for MCP definition validation.
- **`org.opencontainers.image.source`** label on the Docker image.
- **`package.json` metadata** — `mcpName`, `homepage`, `bugs`, `author`, `packageManager`.

## Changed

- **README rewritten** with badges, per-tool documentation, a getting-started guide, a configuration table, Docker instructions, and the project structure.
- **`server.json` name is `io.github.cyanheads/shift-mcp-server`**, and `runtimeHint` moves from `node` to `bun`.
- **The active-workers table escapes pipe characters** in gists and file paths so a `|` no longer breaks the markdown row.

## Removed

- **Echo scaffold tests**, superseded by the real implementation.
