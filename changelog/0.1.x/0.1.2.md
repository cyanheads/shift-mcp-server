---
summary: "Unit test coverage for both tools, the worker store, and the status resource."
breaking: false
security: false
---

# 0.1.2 — 2026-03-23

## Added

- **Unit tests** for `shift_check_in` (new sessions, updates, patch semantics, format output), `shift_check_out` (handler, error cases, format output), the worker store (`generateWorkerId`, `formatWorkersTable`, the `workers` map), and `shift://status` (handler, `list`, `format`).

## Changed

- **`package.json` keywords** gain `ai-agents`, `agent-coordination`, and `typescript`.
